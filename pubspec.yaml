name: workaxis
description: "WorkAxis - Enterprise Workforce & Branch Management"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  intl: ^0.20.2
  equatable: ^2.0.7
  provider: ^6.1.2
  http: ^1.2.2
  sendotp_flutter_sdk: ^0.0.2
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
